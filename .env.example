# Oracle Database Test Credentials
# ================================
#
# 使用說明：
# 1. 複製此檔案為 .env（實際檔名）
# 2. 將下方的帳號密碼替換為實際的 Oracle 資料庫憑證
# 3. 確保 .env 檔案已加入 .gitignore，避免提交到版本控制
#
# 這些憑證用於：
# - 資料庫初始化腳本 (init-scripts/01_setup.sh)
# - Spring Boot 應用程式連線
# - Docker healthcheck 驗證（確保資料庫處於 READ WRITE 狀態）
#
# 安全注意事項：
# ⚠️  絕對不要將實際的 .env 檔案提交到 Git
# ⚠️  在生產環境使用強密碼
# ⚠️  在 CI/CD 環境中，這些應該儲存為 GitHub Secrets
# ⚠️  healthcheck 使用這些變數以避免密碼暴露在日誌中
#
# CI/CD 設定：
# 在 GitHub Actions 中，需要設定以下 Secrets：
# - DB_USER (對應 ORACLE_TEST_USERNAME)
# - DB_PASSWORD (對應 ORACLE_TEST_PASSWORD)

ORACLE_TEST_USERNAME=your_real_username
ORACLE_TEST_PASSWORD=your_real_password

# Spring Boot API HTTP Basic 認證
# ================================
# 後端在 PR #66 後對 /account、/product、/order 等端點加上 HTTP Basic 認證。
# E2E 為外部呼叫端，應使用 api 帳號（internal 帳號僅供 backend loopback 自呼叫）。
# 未設定時，Playwright 與後端容器皆會落回預設值 api / local-api-secret，可開箱即用。
# CI 環境請於 GitHub Secrets 設定 API_USERNAME / API_PASSWORD（正式環境用強密碼）。
API_USERNAME=api
API_PASSWORD=local-api-secret