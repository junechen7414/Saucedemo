name: Saucedemo UI Tests

on:
  # trunk-based 下小改直接推 main
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]
  workflow_dispatch:

permissions:
  contents: read
  actions: read

jobs:
  test:
    timeout-minutes: 60
    runs-on: ubuntu-latest
    
    # 使用 Playwright 官方映像檔，省去 CI 期間安裝瀏覽器與系統依賴的時間
    container:
      image: mcr.microsoft.com/playwright:v1.57.0-noble
      options: --user 0

    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Install pnpm
        uses: pnpm/action-setup@v6
        with:
          version: 10

      # 取得 pnpm store 路徑，用來快取
      - name: Get pnpm store directory
        shell: bash
        id: pnpm-cache
        run: |
          echo "STORE_PATH=$(pnpm store path --silent)" >> $GITHUB_OUTPUT

      # 僅快取 pnpm store 而非 node_modules，避免解壓縮大量細碎檔案的 I/O 損耗
      - name: Setup pnpm cache
        uses: actions/cache@v5
        with:
          path: ${{ steps.pnpm-cache.outputs.STORE_PATH }}
          key: ${{ runner.os }}-pnpm-store-${{ hashFiles('**/pnpm-lock.yaml') }}
          restore-keys: |
            ${{ runner.os }}-pnpm-store-

      - name: Install dependencies
        # --frozen-lockfile 確保測試環境的一致性，並預期加快安裝速度
        run: pnpm install --frozen-lockfile

      - name: Run Playwright tests
        run: pnpm playwright test --project=ui-staging
        env:
          # 告知 Playwright 直接使用容器內的瀏覽器，跳過版本檢查與下載邏輯
          PLAYWRIGHT_SKIP_BROWSER_DOWNLOAD: 1
          # (選用) 如果要更改報告路徑，可透過此環境變數覆寫預設的 playwright-report
          PLAYWRIGHT_HTML_REPORT: custom-report-folder
      
      - name: Upload Report
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: playwright-report
          path: custom-report-folder/  # 這裡必須與上方的 PLAYWRIGHT_HTML_REPORT 一致
          retention-days: 30